# ============================================
# Local Development Environment Variables
# ============================================
# Copy this file to .env and fill in your actual Azure credentials
# .env is gitignored and will NOT be committed to git
#
# Get credentials using Azure CLI (see LOCAL_DEV_SECRETS.md):
#   az storage account show-connection-string --name cvanalyzerdevp3nfnnux --resource-group rg-cvanalyzer-dev
#   az cognitiveservices account keys list --name ai-cvanalyzer-dev --resource-group rg-cvanalyzer-dev
#   az cognitiveservices account keys list --name cvanalyzer-dev-docintel --resource-group rg-cvanalyzer-dev
# ============================================

# Azure Storage (Blob + Queue)
# Get from: az storage account show-connection-string --name cvanalyzerdevp3nfnnux --resource-group rg-cvanalyzer-dev --query connectionString --output tsv
AZURE_STORAGE_CONNECTION_STRING=<YOUR_AZURE_STORAGE_CONNECTION_STRING>

# Azure OpenAI (GPT-4o)
# Get from: az cognitiveservices account keys list --name ai-cvanalyzer-dev --resource-group rg-cvanalyzer-dev --query key1 --output tsv
AGENT_API_KEY=<YOUR_AZURE_OPENAI_API_KEY>

# Azure Document Intelligence (PDF text extraction)
# Get from: az cognitiveservices account keys list --name cvanalyzer-dev-docintel --resource-group rg-cvanalyzer-dev --query key1 --output tsv
DOCUMENT_INTELLIGENCE_API_KEY=<YOUR_DOCUMENT_INTELLIGENCE_API_KEY>

# ============================================
# Notes
# ============================================
# - SQL Server runs in Docker container (no external credentials needed)
# - Endpoints are configured in docker-compose.yml (no need to duplicate here)
# - Production uses Managed Identity (no .env file needed in Azure)
